#include <iostream>
using namespace std;

int main() {
	int n, m;
	while (cin >> n >> m) {
		string op;
		int d;
		cin >> op >> d;
		double single = 0;
		if (op == "1") {
			single = 1.0 * d / 1;
		}
		else if (op == "2") {
			single = 
		}
	}
	return 0;
}